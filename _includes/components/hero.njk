<div class="">
    <div class="jumbotron jumbotron-fluid mt-5 hero-internal mb-0" style="background: url('{{header.image}}') no-repeat center center
    fixed;">
        <div class="container">
            <div class="row">
                <div class="col-8">
                    <h2 class="section-title mb-3 mt-5 hero-title">{{header.title}}</h2>
                        <div class="hero-text">
                        {{header.text}}
                        </div>
                    <div>
                        <a href="{{ header.button.link}}" class="btn btn-primary sm mt-3 " target="_new">{{ header.button.text }}</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>